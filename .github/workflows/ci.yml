name: Run CI

on:
  push:
    branches:
      - master
    paths:
      - "formulae/**"
      - "tests/**"
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    paths:
      - "formulae/**"
      - "tests/**"

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        pixi-env: ["py311", "py312", "py313"]

    name: CI on ${{ matrix.pixi-env }}
    steps:
      - name: Checkout source
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: prefix-dev/setup-pixi@v0.9.4
        with:
          pixi-version: v0.63.2
          environments: ${{ matrix.pixi-env }}

      - name: Run pre-commit checks
        run: pixi run -e ${{ matrix.pixi-env }} pre-commit run --all

      - name: Run Pylint
        run: pixi run -e ${{ matrix.pixi-env }} pylint formulae

      - name: Run tests with Coverage
        run: pixi run -e ${{ matrix.pixi-env }} pytest -vv --cov=formulae --cov-report=term --cov-report=xml tests

      - name: Check Coverage Report
        run: pixi run -e ${{ matrix.pixi-env }} coverage report -m

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          env_vars: OS,PYTHON
          name: codecov-umbrella
          fail_ci_if_error: false
